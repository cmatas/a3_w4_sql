var config = {
  user : "root",
  password : "root", //leave blank for windows
  port: 8889,
  host: "localhost",
  database: "cooperstuff", // database

  kidsmode : false
}

module.exports = config;
